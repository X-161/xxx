# 四周学习计划

建议每周投入 4～6 小时。每个阶段都先理解概念，再完成最小代码，最后记录实验。

## 第 1 周：Python 与串口基础

目标：理解串口配置和 Python 项目环境。

- 学习波特率、数据位、停止位、奇偶校验和超时
- 创建虚拟环境并安装依赖
- 查看 Windows 设备管理器中的串口号
- 设计一条文本数据帧，例如 `TEMP,25.6\n`
- 画出“设备 → 串口 → 解析 → 存储”的数据流

验收：

- 能说明错误波特率可能产生什么现象
- 能区分文本数据和原始字节
- 提交一份自己的协议笔记

## 第 2 周：安全打开串口

目标：完成最小串口生命周期管理。

- 列出可用串口
- 通过命令行指定端口和波特率
- 使用异常处理打开串口
- 确保退出时关闭串口
- 处理无设备、端口占用和读取超时

验收：

- 程序不会因端口不存在而直接崩溃
- 能解释 `timeout` 的作用
- 至少记录三种失败场景

## 第 3 周：数据解析与测试

目标：将一行数据转换为结构化记录。

- 解析 `TYPE,VALUE\n` 格式
- 检查字段数量、类型与合理范围
- 将串口读取和数据解析拆成不同函数
- 为正常、缺字段、非数字和越界数据编写测试

验收：

- 错误数据不会进入有效数据列表
- 解析函数不依赖真实硬件即可测试
- 至少完成 6 个测试用例

## 第 4 周：保存与可视化

目标：形成一个可演示的最小项目。

- 为每条记录添加时间戳
- 保存为 CSV
- 用 Matplotlib 绘制实时曲线
- 增加日志和优雅退出
- 补充 README 的运行截图与实验结果

验收：

- 连续运行 10 分钟无明显异常
- CSV 能被 Excel 正确打开
- 能说明采样频率、刷新频率和性能之间的关系

## 每次实验记录模板

- 日期：
- 本次目标：
- 使用设备或模拟数据：
- 预期结果：
- 实际结果：
- 错误信息：
- 原因分析：
- 下一步：
